server:
  port: 6996

spring:
  application:
    name: demo-api
  config:
    activate:
      on-profile: local
  datasource:
    url: 'jdbc:mysql://localhost:3306/demo'
    username: root
    password: 123456
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://${env.spring.datasource}/demo?useUnicode=yes&serverTimezone=Asia/Ho_Chi_Minh&characterEncoding=UTF-8

env:
  api:
    via: K
  local: 10.26.53.221:${server.port}
  spring:
    datasource: 127.0.0.1
  vndid:
    api: https://accounts-uat.vndirect.com.vn
  crms:
    url: https://accounts-suat.vndirect.com.vn
  host:
    ip: localhost
    scheme: http
  testing-endpoint.enable: true
constant:
  zalo:
    template-id: -9999
---